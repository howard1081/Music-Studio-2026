name: sanity

on:
  push:
  pull_request:

jobs:
  sanity:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4

      - name: Run sanity
        shell: pwsh
        run: .\run.ps1
